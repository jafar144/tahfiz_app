import 'package:khoirunnasyien/main_khoirunnasyien.dart' as app;

/// Entry point default untuk developer. Build release tetap harus menyebutkan
/// `--flavor`, target `main_<flavor>.dart`, dan `APP_FLAVOR` secara eksplisit.
Future<void> main() => app.main();
